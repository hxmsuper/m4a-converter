# m4aToMp3## 使用说明
1. 运行应用程序
2. 拖拽或点击"添加文件"按钮选择.m4a文件
3. 点击"选择..."按钮选择输出目录
4. 选择目标格式（opus/mp3，默认opus）
5. 点击"开始转换"进行转换
6. 等待转换完成，进度条会显示进度

## 依赖
- .NET 9.0或更高
- FFmpeg（需要将ffmpeg.exe放在程序目录下）

## 下载FFmpeg
从 https://ffmpeg.org/download.html 下载Windows版本的FFmpeg，将ffmpeg.exe复制到程序运行目录下。

**注意：** ffmpeg.exe文件较大（约94MB），未包含在仓库中，请自行下载。

## 构建
```bash
dotnet build
```

## 运行
```bash
dotnet run
```和FFmpeg的轻量级Windows音频转换工具，支持将.m4a批量或单个转换为.mp3。

## 功能
- 支持批量和单个文件转换
- 拖拽添加文件
- 可自定义输出目录
- 支持多种目标格式（opus/mp3）
- 转换进度可视化
- 集成FFmpeg命令行调用

## 使用说明
1. 选择或拖拽.m4a文件到界面
2. 选择输出目录
3. 点击“开始转换”

## 依赖
- .NET 6.0或更高
- FFmpeg（随程序一同分发）

## 计划
- [ ] 支持批量和单个文件转换
- [ ] 拖拽添加文件
- [ ] 可自定义输出目录
- [ ] 转换进度可视化
- [ ] 集成FFmpeg命令行调用
